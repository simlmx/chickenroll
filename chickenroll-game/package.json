{
  "name": "chickenroll-game",
  "version": "1.0.0",
  "description": "",
  "main": "dist/cjs/index.js",
  "types": "dist/types/index.d.ts",
  "module": "dist/esm/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "rollup --config && tsc --emitDeclarationOnly",
    "watch": "rollup --config --watch",
    "watch2": "tsc --emitDeclarationOnly --watch",
    "test": "jest --verbose"
  },
  "author": "Simon Lemieux",
  "license": "ISC",
  "dependencies": {
    "bgkit": "^1.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^18.0.0",
    "@rollup/plugin-node-resolve": "^11.2.1",
    "@rollup/plugin-typescript": "^8.2.1",
    "@types/jest": "^26.0.23",
    "jest": "^27.0.0-next.8",
    "rollup": "^2.45.2",
    "ts-jest": "^27.0.0-next.11",
    "tslib": "^2.2.0",
    "typescript": "^4.1.5"
  },
  "jest": {
    "preset": "ts-jest/presets/default-esm",
    "globals": {
      "ts-jest": {
        "useESM": true
      }
    }
  }
}
